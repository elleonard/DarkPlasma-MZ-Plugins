{
  "name": "build",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "build": "run-s clean build:*",
    "build:ts": "tsc",
    "watch": "tsc -w",
    "test": "vitest run",
    "update-snapshot": "vitest run --update",
    "clean": "rimraf ./lib"
  }
}
