/// <reference path="../../../typings/rmmz.d.ts" />

declare interface Bitmap {
  fillGradientCircle(centerX: number, centerY: number, radius: number, insideColor: string, outsideColor: string): void;
}
